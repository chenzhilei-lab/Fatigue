# 齿轮疲劳 v4.0 → 投稿就绪 — 提升框架
# 2026-08-05

---

## 〇、当前状态诊断

论文 v4.0 已通过 30+ 轮修改，核心架构稳定。剩余问题分三层：

| 层级 | 问题 | 致命程度 | 工作量 |
|------|------|:--:|--------|
| **地基** | Basquin 不自洽（679/626 ≠ 610/610） | 🔴 | 重跑全管线 |
| **数据** | output/ 多版本 JSON 混放 | 🔴 | 清理 + MANIFEST |
| **文本** | 3-method/4-method 数字残留在少数段落 | 🟡 | 逐段扫描 |
| **诚信** | 679/626 来源标注不实（Excel≠SMDIdbase） | 🟡 | 修改来源声明 |

---

## 一、地基修复（必须先做，否则一切数字都是错的）

### 问题
`fatigue_models.py` 用 `fatigue_strength_at_1e6 = 679/626` 做 run-out 门槛，
但这两个值来自本地 SAE Excel，与论文声称的 Waterloo SMDIdbase 不符，
且与同源的 Basquin 参数（σf′、b）不自洽——
按 Basquin 公式计算，10⁶ 处为 ~610/610 MPa，差 0.5 MPa，不是论文说的 53 MPa。

### 修复路径
```
方案 A（推荐）：用 Basquin 自洽门槛
  fatigue_strength_at_1e6 = σf′ × (2×10⁶)^b
  → Iter68: 610.5 MPa, Iter66: 610.0 MPa
  → 重跑 run_sweep.py → 更新 sweep.json → 重跑全部 AFT/敏感性
  → 论文所有数字更新

方案 B（保底）：保留 679/626，诚实标注来源
  gear_params.py 注释改为 "SAE 材料力学性能测试数据表.xlsx, 非 SMDIdbase"
  论文声明: "experimentally measured at 10⁶ cycles from SAE database"
  标注 Basquin 不自洽为已知限制
```

### 选择标准
方案 A 更干净但数字全变；方案 B 保底但留下"门槛与曲线不自洽"的尾巴。
若投 JMST，方案 B 可接受。若未来再投 IJF，必须方案 A。

---

## 二、数据管线固化（一次性解决版本混用）

### 问题
output/ 目录有 3 个版本的 sweep.json、3 个版本的 aft_anova.json，
多个敏感性 JSON 来自不同运行。Claude 读 A.json 取 52 finite，
读 B.json 取 116 finite——它不知道这是两次运行。

### 修复
```bash
# reproduce.sh 末尾追加
python3 -c "
import json, hashlib, os, datetime
files = ['sweep.json','aft_anova.json','aft_diagnostics.json',
         'ka_sensitivity.json','threshold_sensitivity.json',
         'multi_aft_comparison.json','native_stress_sensitivity.json',
         'weibull_aft.json','sobol_vs_aft.json']
manifest = {
    'run_id': datetime.datetime.now().isoformat(),
    'command': 'bash reproduce.sh',
    'files': {}
}
for f in files:
    path = f'output/{f}'
    if os.path.exists(path):
        manifest['files'][f] = hashlib.md5(open(path,'rb').read()).hexdigest()
json.dump(manifest, open('output/MANIFEST.json','w'), indent=2)
print(f'MANIFEST: {len(manifest[\"files\"])} files hashed')
"
```

然后 `paper_check.py` 新增规则：
```python
# 跨文件版本一致性检查
def check_manifest_consistency(citations, manifest_path):
    """论文中引用的所有数字只能来自同一个 MANIFEST"""
    pass  # 违反 → VERSION_MISMATCH
```

---

## 三、公式审计（套用 GAIA formula_audit_SOP）

对标 `formula_audit_SOP.md`，逐公式验证：

| 编号 | 代码函数 | 引用来源 | PDF 已下载 | 验证状态 |
|------|---------|---------|:--:|:--:|
| F1 | `compute_nominal_stress` | ISO 6336-3:2019 §Method B | ✅ | 待逐行对 |
| F2 | `apply_mean_stress_correction` | Stephens 2001 Ch.4 | | 4公式已验证 |
| F3 | `apply_size_surface_factor` (ISO) | ISO 6336-3:2019 §6.2, §6.4 | ✅ | YX 近似待确认 |
| F4 | `apply_size_surface_factor` (AGMA) | AGMA 2001-D04 §15, §16 | | Cf 公式已验证 |
| F5 | `apply_size_surface_factor` (FKM) | FKM 7th ed. §4.3, §4.5 | ✅ | Kd 简化待确认 |
| F6 | `compute_Kt_geometric` | ~~Dolan & Broghamer 1942~~ | | 代码自标"引用错误" |
| F7 | `compute_Kf` (Peterson) | Peterson 1974 | | 已算 Kf≈1.84 |
| F8 | `compute_Kf` (Neuber) | Neuber 1958 | | 已算 Kf≈1.75 |
| F9 | AGMA J-factor 查表 | AGMA 2001-D04 Fig.8 | | 论文用 [0.31,0.35] |
| F10 | SWT 实现 | Smith-Watson-Topper 1970 | | 代码用应力简化版 |

### 执行方式
- 对每个公式：PDF 原文 → 代码逐行 → 标注 ✅/❌/⚠️
- 未下载 PDF 的公式：下载 → 验证 → 更新状态
- 发现不一致：修正代码 → 重跑 → 更新数字

---

## 四、论文文本最终扫描

### 4.1 数字一致性扫描
```bash
# 找论文中所有百分比数字，逐一与 JSON 核对
grep -oP '\d+\.\d+\s*\\%' paper/gear_fatigue_v4.0.tex | sort -u > /tmp/paper_pct.txt
# 与 aft_anova.json / multi_aft_comparison.json 逐行对比
```

### 4.2 3-method/4-method 混用残留
搜索模式：`39.1|35.3|23.5|97.9|64%|92 run-out|144 comb`
（这些是 4-method 数字，在 3-method 语境下应被替换）

### 4.3 绝对化声称最终扫描
搜索：`No prior|the first|never been|unprecedented|All authors`

### 4.4 跨文件引用一致性
每个 `\ref{sec:XXX}` 和 `\ref{tab:XXX}` 必须对应一个实际存在的 `\label{XXX}`

---

## 五、投稿前强制清单

借鉴 GAIA `audit_submit.sh`：

| # | 检查项 | 状态 |
|---|--------|:--:|
| 1 | reproduce.sh 能从头跑到尾，输出与论文数字一致 | [ ] |
| 2 | MANIFEST.json 存在，所有引用数字来自同一 run_id | [ ] |
| 3 | formula_audit 全部标注 ✅/❌/⚠️，❌已修正 | [ ] |
| 4 | 论文中无 "No prior" / "the first" / "All authors" | ✅ |
| 5 | 所有 `\ref` 标签存在 | [ ] |
| 6 | 参考文献 URL 未截断 | [ ] |
| 7 | Du et al. 文章号正确 (106578, 非 106575) | [ ] |
| 8 | 3-method/4-method 数字无混用 | [ ] |
| 9 | 风险阈值表标注 "Methodology demonstration — NOT for design" | ✅ |
| 10 | Cover letter 无 "No prior" / "All authors" | ✅ |

---

## 六、执行顺序

```
第1步（1小时）：方案选择（A vs B）+ MANIFEST 脚本加到 reproduce.sh
第2步（1小时）：公式审计 → F1-F10 标注 ✅/❌/⚠️
第3步（2小时）：重跑 sweep（如选方案A）+ 更新全部 JSON
第4步（1小时）：论文数字最终扫描 + 残留 4-method 数字替换
第5步（30分）：投稿前清单逐项打勾
```

总工时：~5-6 小时集中工作。

---

## 附录：不做的优化（防止过度修改）

- ❌ 不重写文献综述
- ❌ 不删风险分级表（已有 "NOT for design" 标注）
- ❌ 不在每张图加免责后缀
- ❌ 不执行 GAIA 方案中的 "全部图表标注下限"
- ❌ 不补实体试验（Phase 2 承诺已足够）
