# 齿轮疲劳论文 v4.0 — 审计进度存档（2026-08-04）

> 任务: 代码层 + 数据层完整审计（共三件事）。本文件只完成了**第一件（代码层审计）**的绝大部分，
> 第二件（数据版本）、第三件（完整清单 审稿问题清单_齿轮_v4.0.txt）未做。存档以便切换任务后无缝续做。
> 工作目录: /mnt/d/Papers/0723-Gear_Fatigue_UQ ；论文: paper/gear_fatigue_v4.0.tex

## 一、已核实的代码事实

### 1. Basquin 参数与 679/626 的来源（问题 a）
- `paper/ref_papers/Iter_068.pdf` 附录: σ′f = 1355.7 MPa, b = −0.055, UTS = 1355.1 MPa, HRC 38 → 代码 1356/−0.055 ✓
- `paper/ref_papers/Iter_066.pdf` 附录: σ′f = 1684.2 MPa, b = −0.070, UTS = 1537 MPa, HRC 42 → 代码 1684.0/−0.070 ✓
- `paper/ref_papers/材料力学性能测试数据表.xlsx`（SAE 棒钢库，Sheet1）第 74 行 Iter68: Fat.Str.@N=10^6 = 679 MPa；
  第 72 行 Iter66: 626.3 MPa（列头在 row3-4: Fat.Str.@ N=10^6 / YS / UTS / %RA / %ELong / BHN）
- **结论**: σf′/b 来自 Waterloo SMDI PDF；679/626.3 来自本地 Excel（SAE 数据），按迭代号 68/66 对应。
  `gear_params.py` 的 source 注释属实；但论文可见文本（L284-299 及 L1024-1034）把 679/626 说成
  “drawn from SMDIdbase” / “experimentally measured ... for Waterloo #68/#66”，未披露 Excel → 审稿人可见的来源归属错误。
- 材料不匹配: 论文/代码 MATERIAL UTS=1000 MPa、~300 HB（42CrMo4），但两个数据集的实测 UTS 是 1355.1 / 1537 MPa
  （HRC 38/42，Excel BHN 352/388）。Goodman/Gerber/Peterson 的 uts 参数与 S-N 数据来源材料不一致。

### 2. Basquin 自洽性（问题 b）— 核心数字矛盾
- 按论文 eq:basquin σ(10^6) = σf′·(2×10^6)^b:
  - Iter68: 1355.7 × (2e6)^(−0.055) ≈ **610.5 MPa** ≠ 679（差 −68.5 MPa, −10.1%）
  - Iter66: 1684.2 × (2e6)^(−0.070) ≈ **610.0 MPa** ≠ 626.3（差 −16.3 MPa, −2.6%）
- 两条拟合曲线在 10^6 处几乎重合（610.5 vs 610.0，差 ~0.5 MPa）；论文 L297-299 “53 MPa 差值、不同斜率导致相近 run-out 阈值”
  的说法在 Basquin 曲线下不成立（真正相近是因为 1e6 交叉点本就几乎相同）。
- 代码行为（fatigue_models.py compute_fatigue_life_sn）: 用 Excel 值 679/626.3 作 run-out 门槛，门槛以上用 Basquin；
  曲线在门槛处不连续: σ=679.01 MPa 时 Basquin 给出 Nf ≈ 1.4×10^5（Iter68）；σ=626.3 时 Nf ≈ 6.9×10^5（Iter66），
  即“10^6 疲劳强度”与同源 Basquin 曲线不符（应 ≈610）。
- 影响: 所有 run-out 计数、AFT 删失、K_A/Y_Sa 敏感性都建立在这个不一致的门槛上。

### 3. ka_sensitivity.py run-out→finite 分支（问题 c）
- `recompute_nf_with_ka`: 顶部 `if entry.get("log10_Nf") is None: return "inf", None` 已把全部 run-out 提前返回；
  后面注释为 “was run-out but now finite-life” 的分支（检查 entry["Nf"]=="inf"）**不可达 = 死代码**；
  且即使可达也返回 "inf"，即 K_A>1 永远不能把 run-out 转为 finite，物理上错误（应力升高应使部分 run-out 变有限寿命）。
- K_A 缩放 `log10(Nf)+log10(KA)/b`、`σ_eff×KA` 仅在应力线性缩放时精确；Goodman/Gerber/Morrow 的 σar 对 σmax 非线性（见 §4）。

### 4. Y_Sa 偏差计算（问题 d）
- `ysa_sensitivity.py`: 对 AGMA/FKM 条目用 `log10(Nf)+log10(factor)/b`，等价于“σ_eff 线性缩放”，仅对 SWT 精确。
  以 Goodman、factor=1.33、σF0=778 MPa、UTS=1000 为例: 精确 σar 比 1072/637 ≈ 1.68，简化取 1.33（低估 ~27%）；
  寿命差 ~1.9 dex（约 70 倍），**不是论文 L509 所称 “sub-percentage-point rounding”**。
- `native_bias_propagation.py`: 走完整管线重算（正确做法），但:
  (i) 硬编码 `baseline_std_var_pct: 30.1` 并打印 “Baseline (uniform Y_Sa): 30.1%”——30.1% 是 0723-工作进度.txt 里旧版数字，
  与论文 43.7%（108 组合）/ 39.1%、35.3%（144 组合）不一致 → 版本混用标记；
  (ii) AFT 分母 `total = sum(drops.values()) + di`（主效应未取 max(0,·)），而 aft_variance_decomposition.py 取 max(0,·)，
  ka_sensitivity.py 取 max(0,·)，ysa_sensitivity.py 主效应不取、交互取 → 三个脚本三种分母约定，同一数据会得到不同方差占比；
  (iii) recompute_entry_agma_native 硬编码 Ft = 2×700×1000/d1（700 N·m，与当前 LOAD 一致）。
- 与论文一致的数值: Y_Sa=1.55；J=0.33 → 1/J=3.03；ISO 乘子 YFa·YSa·Yε=3.00；(1/J)/(YFa·Yε)=1.57（+1.3%，L444-447）；
  Peterson Kf≈1.84（Kt=1.913、a=0.0949、r=1.14）与 fatigue_models.compute_Kf 一致（L448-450）。

### 5. 组合数（问题 e）
- 3×3×2×3×3 = **162** ≠ 108 ≠ 144（用户给的式子与论文两个口径都不等）。
- 论文实际: 2×3×2×3×3 = **108**（L691、L735，3 个平均应力方法 Gerber/Morrow/SWT）；
  2×4×2×3×3 = **144**（L525，含 Goodman）——这两个算式本身正确。
- `run_sweep.py` 文档字符串 “3×3×4×2×3×3 = 972” **数学错误**（乘积=648），且实际代码跑 2×4×2×3×3=144；
  打印还写 “350 N·m”，与 gear_params LOAD 700 N·m、论文 700 N·m 矛盾（sweep_summary.json 也写 torque_Nm=350）。
- `gear_params.py` 自检 2×2×4×2×3×3=288 正确（但含已删除的 Kf 开关）。
- 历史版本组合数漂移: 工作进度记录过 324 / 648 / 972 / 288 → 输出 JSON 有版本混放风险（第二件待查）。

### 6. 其它公式核对
- ISO 6336-3 Method B σF0 = Ft/(b·mn)·YFa·YSa·Yε·Yβ: 代码正确；700 N·m → Ft=23.33 kN → σF0=777.7≈778 MPa ✓（与论文 L481 一致）。
  但 YFa=3.06−0.013z、YSa=1.58−0.0015z 是线性近似，非标准公式本身（标准值依赖刀具几何/变位）；论文引 Annex A Fig A.1（待复核标准 PDF）。
- Yε=0.25+0.75/1.7=0.691 ✓
- ISO YRrelT = 1.674−0.529·(Rz+1)^0.1（1≤Rz≤40）✓；Rz>40 截断到 40 值（代码用 41^0.1 → 0.907）✓
- ISO YX: 代码 “mn≤10 → 1.0” 疑似与标准 §6.2 不符（标准 YX=1.03−0.006·mn，5≤mn≤30；mn=10 时=0.97）；mn=3 结果同为 1.0（待复核）
- AGMA: Ks=1.0（Pd≥5）✓；Cf=1−0.0058·ln(Rf[μin])·ln(Sut[ksi])（§16）✓ 结构；Rz≤8 → 磨削 Cf=1.0；Kr=1.0（99% 可靠度）✓
- FKM: KFσ=1−0.22·log10(Rz)·log10(2·UTS/400)（§4.3）✓ 结构；Kd=1−0.05·log10(10·mn)=0.926 是简化式，非 FKM §4.5 原式
  （证据链映射表 证据链-标准查证映射表.md 自身标 ⚠️近似）
- 平均应力: Goodman/Gerber/Morrow 公式正确；SWT 代码实现 σar=sqrt(σmax·σa) 是应力版简化（真 SWT 含 εa·E），论文需注明
- Miner_modified Dc=0.7 标注 “AGMA/FKM 推荐 0.7” 存疑（AGMA 2001 / FKM 无此表述），待核实出处
- fatigue_models.py: `verbose=True` 分支引用未定义变量 Kf/sigma_max → NameError（自测会崩）；
  compute_Kf / compute_Kt_geometric 自 v2.3 起管线未调用（死代码），但论文 FKM native 检查 Kf≈1.84 用的正是该未调用函数
- aft_variance_decomposition.py: “方差占比”实为 ΔlogL 归一化（非真实方差分解），分母 max(0,Δ) 约定任意；
  bootstrap 2000 次 / seed 42 与论文一致（待对 JSON 数值）

## 二、尚未完成（下一步续做清单）
- 第二件: output/ 多版本 JSON 对比（sweep.json vs sweep.json.bak vs sweep_v3.4_backup.json；
  aft_anova.json vs .bak vs _v3.4_backup；ysa_sensitivity.json vs ysa_sensitivity_full.json；各敏感性 JSON 与论文数字逐一比对），
  确认主分析是 108 还是 144 组合
- 第三件: 输出完整清单 审稿问题清单_齿轮_v4.0.txt（A 规范值 / B 数字错误 / C 表述问题 / D 复现包卫生 / E 修正步骤，含论文行号）
- 待复核: ISO 标准 PDF 中 YFa/YSa/YX 原文页；FKM “Dc=0.7” 出处；sweep.json 实际 sigma_nominal（700 还是 350 N·m 生成）；
  ka/ysa JSON 与论文 L499-516 数字（38.8→40.1% 等）是否一致

## 三、关键文件路径
- 代码: fatigue_models.py / gear_params.py / run_sweep.py / aft_variance_decomposition.py /
  ka_sensitivity.py / ysa_sensitivity.py / native_bias_propagation.py / Y_Sa_sensitivity.py / Y_Sa_sensitivity_full.py
- 数据: paper/ref_papers/Iter_068.pdf、Iter_066.pdf、材料力学性能测试数据表.xlsx；output/*.json（含 .bak / *_backup.json 混放）
- 论文: paper/gear_fatigue_v4.0.tex（2550 行）
